# AGENTS.md

## UI verification

For every UI change, verify the rendered result in a browser at **both desktop and mobile breakpoints** before reporting completion. Inspect the actual visual presentation (not just DOM/text), including clipping, overflow, contrast, interactive states when applicable, and responsive layout.
